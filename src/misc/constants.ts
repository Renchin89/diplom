export enum MainTab {
  DASHBOARD = "dashboard",
  CALENDAR = "calendar",
}

export enum OrganizationTab {
  EMPLOYEE = "employee",
  STRUCTURE = "structure",
  SETTINGS = "settings",
  REPORT = "report",
}

export enum RecruitmentTab {
  CANDIDATES = "candidates",
}