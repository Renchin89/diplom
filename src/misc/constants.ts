export enum MainTab {
  EMPLOYEE = "Employee",
  TASK = "Task",
}

export enum OrganizationTab {
  ADDEMPLOYEE = "Add Employee",
  RISK = "Risk",
  SETTINGS = "settings",
  REPORT = "report",
}

export enum RecruitmentTab {
  CANDIDATES = "Login",
}