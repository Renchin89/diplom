export enum MainTab {
  EMPLOYEE = "Employee",
  WORK = "Work",
}

export enum OrganizationTab {
  ADDEMPLOYEE = "Add Employee",
  RISK = "Risk",
  SETTINGS = "settings",
  REPORT = "report",
}

export enum RecruitmentTab {
  CANDIDATES = "candidates",
}