export enum MainTab {
  DASHBOARD = "db",
  CALENDAR = "cl",
}

export enum OrganizationTab {
  EMPLOYEE = "em",
  STRUCTURE = "sr",
  SETTINGS = "st",
  REPORT = "rp",
}

export enum RecruitmentTab {
  CANDIDATES = "cd",
}
